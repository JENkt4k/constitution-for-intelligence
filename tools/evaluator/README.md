# Evaluator

Planned reference evaluator.

The first implementation should:
- load `constitution/constitution.yaml`;
- validate scenario documents;
- emit invariant checks and structured rationale;
- support symmetry transforms;
- preserve decision logs;
- never self-amend the constitution.
